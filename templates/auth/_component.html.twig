<turbo-frame
        id="login" src="{{ path('app_login') }}"
        class="d-flex align-items-center justify-content-center border border-1 border-secondary py-3 px-3 rounded-5"
>
    {{ include('shared/loaders/_spinner.html.twig') }}
</turbo-frame>