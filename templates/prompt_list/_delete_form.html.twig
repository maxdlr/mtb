<form method="post" action="{{ path('app_prompt_list_delete', {'id': prompt_list.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prompt_list.id) }}">
    <button class="btn">Delete</button>
</form>
